# Mini-Codex-Reliability-Lab.
Minimal sandbox for studying how tool-using coding agents fail on real bugfix tasks. Ten isolated workspaces, five tools, a batch eval runner, and optional Docker isolation. Built to make failure modes obvious, not hide them.
